---
- hosts: localhost
  become: true
  vars:
    arch_mapping:
      x86_64: amd64
      aarch64: arm64

  tasks:
    # Gather facts about the system
    - name: Get system facts
      setup:

    - name: Update package lists
      apt:
        update_cache: yes

    - name: Install required system packages
      apt:
        name:
          - ca-certificates
          - curl
          - software-properties-common
        state: present

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker repository
      apt_repository:
        repo: "deb [arch={{ arch_mapping[ansible_architecture] }}] https://download.docker.com/linux/ubuntu {{ ansible_distribution_major }} stable"
        state: present

    - name: Install Docker
      apt:
        name: docker-ce
        state: present

    - name: Start and enable Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Verify Docker installation
      debug:
        msg:
          - Docker version is {{ docker_version.version }}

    - name: Install Node.js
      apt:
        name: nodejs

    - name: Verify Node.js installation
      debug:
        msg:
          - Node.js version is {{ node_version }}
          
          